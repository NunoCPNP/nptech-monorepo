import { colors } from '@nptech/styles'

export const dark = {
  colors: {
    dark: `${colors.darkGrey}`,
    light: `${colors.lightGrey}`,
    highlight: `${colors.yellow}`,
    white: `${colors.white}`,
    black: `${colors.black}`,
  },
}

// TODO -> LIGHT THEME
export const light = {
  colors: {},
}
